html
    head
        title User Manager
    body
        h1 Edit User
        form(id="myForm", method="post", action=`/edit/${userId}`)
            input(type="text", id="firstName", name="firstName", value=`${firstNameField}`, required)
            br
            input(type="text", id="lastName", name="lastName", value=`${lastNameField}`, required)
            br
            input(type="email", id="email", name="email", value=`${emailField}`, required)
            br
            input(type="number", id="age", name="age", value=`${ageField}`, required)
            br
            input(type="submit", value="Save User")